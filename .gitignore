# Internal BBL source documents and their derived working files
# (cleaned intermediate Markdown, indexes, conversion logs). Kept locally
# only — REQUIREMENTS.md references their content but the files themselves
# are not published.
docs/BBL Requierements/
